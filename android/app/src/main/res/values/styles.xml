<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 主主题：深色背景，透明状态栏 -->
    <style name="Theme.NativeTavern" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/background</item>
    </style>

    <!-- Splash Screen 主题 (Android 12+) -->
    <style name="Theme.NativeTavern.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.NativeTavern</item>
    </style>
</resources>